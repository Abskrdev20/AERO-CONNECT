<!DOCTYPE html>
<html>
<head>
<title>Grievance Status | AAI</title>

<style>
/* ===== BASE ===== */
body{
  margin:0;
  font-family:"Segoe UI", Arial, sans-serif;
  background:#f3f6fb;
  color:#1f2937;
}

/* ===== HEADER ===== */
.header{
  background:linear-gradient(90deg,#0b4aa2,#1e6bd6);
  padding:18px 40px;
  display:flex;
  justify-content:space-between;
  align-items:center;
  color:white;
}
.header h2{
  margin:0;
  font-size:22px;
  font-weight:600;
}
.header p{
  margin:4px 0 0;
  font-size:14px;
  color:#ffffff;
}

/* BACK BUTTON */
.back-btn{
  background:white;
  color:#0b4aa2;
  border:none;
  padding:10px 18px;
  border-radius:8px;
  font-weight:600;
  cursor:pointer;
}
.back-btn:hover{
  background:#eaf1ff;
}

/* ===== CONTAINER ===== */
.container{
  max-width:1200px;
  margin:26px auto 60px;
  padding:0 24px;
}

/* ===== STATUS CARD ===== */
.status-card{
  background:white;
  border-radius:18px;
  padding:30px 36px;
  display:flex;
  justify-content:space-between;
  align-items:center;
  box-shadow:0 14px 34px rgba(0,0,0,.08);
}

.status-left h3{
  margin:0 0 6px;
  font-size:22px;
}
.muted{
  color:#6b7280;
  font-size:14px;
}

/* STATUS BADGE */
.status-badge{
  padding:12px 26px;
  border-radius:999px;
  font-weight:700;
  font-size:14px;
  color:#fff;
}
.submitted{background:#64748b}
.review{background:#2563eb}
.progressing{background:#f59e0b;color:#111}
.resolved{background:#16a34a}
.rejected{background:#dc2626}
.escalated{background:#7c3aed}

/* ===== STEPPER ===== */
.stepper{
  margin:34px 0 6px;
  display:flex;
  justify-content:space-between;
  position:relative;
}
.stepper::before{
  content:"";
  position:absolute;
  top:18px;
  left:0;
  right:0;
  height:4px;
  background:#dbe3f3;
  border-radius:3px;
}
.step{
  position:relative;
  background:#f3f6fb;
  padding:0 10px;
  text-align:center;
}
.step span{
  display:block;
  width:34px;
  height:34px;
  border-radius:50%;
  background:#cbd5e1;
  margin:0 auto 6px;
}
.step.active span{
  background:#1e6bd6;
}
.step p{
  margin:0;
  font-size:13px;
  font-weight:600;
}

/* ===== GRID ===== */
.grid{
  margin-top:28px;
  display:grid;
  grid-template-columns: 1.3fr 0.7fr;
  gap:26px;
}

/* ===== CARD ===== */
.card{
  background:white;
  border-radius:18px;
  padding:28px;
  box-shadow:0 10px 26px rgba(0,0,0,.07);
}
.card h4{
  margin:0 0 18px;
  font-size:16px;
  color:#0b4aa2;
  font-weight:700;
}

/* ROW */
.row{
  display:flex;
  justify-content:space-between;
  margin:12px 0;
  font-size:14px;
}
.label{color:#6b7280}
.value{font-weight:600}

/* TIMELINE */
.timeline{
  list-style:none;
  padding:0;
  margin:0;
}
.timeline li{
  padding:12px 0 12px 22px;
  border-left:4px solid #1e6bd6;
  position:relative;
  font-size:14px;
}
.timeline li::before{
  content:"";
  position:absolute;
  left:-9px;
  top:18px;
  width:14px;
  height:14px;
  background:#1e6bd6;
  border-radius:50%;
}

/* REMARK */
.remark{
  background:#eef4ff;
  border-left:6px solid #1e6bd6;
  padding:18px;
  border-radius:10px;
  font-size:14px;
  line-height:1.6;
}

/* ALERTS */
.alert{
  margin-top:18px;
  padding:16px;
  border-radius:12px;
  font-size:14px;
}
.alert.reject{
  background:#fee2e2;
  border-left:6px solid #dc2626;
}
.alert.escalate{
  background:#ede9fe;
  border-left:6px solid #7c3aed;
}

/* FOOTER */
.footer{
  text-align:center;
  margin-top:60px;
  font-size:13px;
  color:#6b7280;
}

/* RESPONSIVE */
@media(max-width:900px){
  .grid{grid-template-columns:1fr}
  .status-card{flex-direction:column;align-items:flex-start;gap:14px}
}
</style>
</head>

<body>

<!-- HEADER -->
<div class="header">
  <div>
    <h2>Airport Authority of India</h2>
    <p>Grievance Tracking Portal</p>
  </div>

  <!-- BACK BUTTON -->
  <button class="back-btn" onclick="window.location.href='/track'">
    ← Back
  </button>
</div>

<div class="container">

  <!-- STATUS CARD -->
  <div class="status-card">
    <div class="status-left">
      <h3>Grievance Status</h3>
      <div class="muted">Token: <b><%= token %></b></div>
      <div class="muted">Submitted on <%= submissionDate %></div>
    </div>

    <div class="status-badge 
      <%= status === 'Resolved' ? 'resolved' :
          status === 'Rejected' ? 'rejected' :
          status === 'Escalated' ? 'escalated' :
          status.includes('Review') ? 'review' :
          status.includes('Progress') ? 'progressing' : 'submitted' %>">
      <%= status %>
    </div>
  </div>

  <!-- STEPPER -->
  <div class="stepper">
    <div class="step active"><span></span><p>Submitted</p></div>
    <div class="step <%= progress>=50 ? 'active':'' %>"><span></span><p>Reviewed</p></div>
    <div class="step <%= progress>=80 ? 'active':'' %>"><span></span><p>In Progress</p></div>
    <div class="step <%= progress===100 ? 'active':'' %>"><span></span><p>Closed</p></div>
  </div>

  <!-- GRID -->
  <div class="grid">

    <!-- LEFT -->
    <div class="card">
      <h4>Grievance Details</h4>

      <div class="row"><span class="label">Department</span><span class="value"><%= department %></span></div>
      <div class="row"><span class="label">Responsible Officer</span><span class="value"><%= officer %></span></div>
      <div class="row"><span class="label">Last Updated</span><span class="value"><%= lastUpdated %></span></div>

      <h4 style="margin-top:26px">Remarks</h4>
      <div class="remark"><%= remark %></div>

      <% if(status === "Rejected"){ %>
        <div class="alert reject">
          <b>Rejected:</b> The grievance could not be processed due to incomplete information.
        </div>
      <% } %>

      <% if(status === "Escalated"){ %>
        <div class="alert escalate">
          <b>Escalated:</b> The grievance has been forwarded to a higher authority due to delay.
        </div>
      <% } %>
    </div>

    <!-- RIGHT -->
    <div class="card">
      <h4>Status History</h4>
      <ul class="timeline">
        <% timeline.forEach(step => { %>
          <li><%= step %></li>
        <% }) %>
      </ul>
    </div>

  </div>

</div>

<div class="footer">
  © 2026 Airport Authority of India | Government of India
</div>

</body>
</html>
