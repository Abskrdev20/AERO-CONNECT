<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Track Grievance - AAI</title>
<style>
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  background: linear-gradient(135deg, #1e3c72 0%, #2a5298 50%, #7e8ba3 100%);
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  position: relative;
  overflow-x: hidden;
}

/* Animated background elements */
body::before {
  content: '';
  position: absolute;
  width: 500px;
  height: 500px;
  background: rgba(255, 255, 255, 0.08);
  border-radius: 50%;
  top: -250px;
  right: -250px;
  animation: float 20s ease-in-out infinite;
}

body::after {
  content: '';
  position: absolute;
  width: 400px;
  height: 400px;
  background: rgba(255, 255, 255, 0.05);
  border-radius: 50%;
  bottom: -200px;
  left: -200px;
  animation: float 15s ease-in-out infinite reverse;
}

@keyframes float {
  0%, 100% { transform: translate(0, 0); }
  50% { transform: translate(30px, 30px); }
}

.header {
  background: rgba(255, 255, 255, 0.15);
  backdrop-filter: blur(10px);
  color: #fff;
  padding: 32px 24px;
  text-align: center;
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
  position: relative;
  z-index: 10;
}

.header h1 {
  font-size: 28px;
  font-weight: 600;
  margin-bottom: 8px;
  text-shadow: 0 2px 4px rgba(0, 0, 0, 0.2);
}

.header p {
  font-size: 15px;
  opacity: 0.95;
  font-weight: 300;
}

.container {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 40px 20px;
  position: relative;
  z-index: 10;
}

.box {
  max-width: 520px;
  width: 100%;
  background: #ffffff;
  padding: 48px 40px;
  border-radius: 24px;
  box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
  animation: slideUp 0.6s ease-out;
  position: relative;
  overflow: hidden;
}

.box::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 4px;
  background: linear-gradient(90deg, #1e3c72, #2a5298);
}

@keyframes slideUp {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.back-button {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  color: #2a5298;
  text-decoration: none;
  font-size: 14px;
  font-weight: 500;
  margin-bottom: 24px;
  transition: all 0.3s ease;
  cursor: pointer;
}

.back-button:hover {
  color: #1e3c72;
  transform: translateX(-4px);
}

.back-button svg {
  width: 18px;
  height: 18px;
  stroke: currentColor;
  fill: none;
  stroke-width: 2;
}

.icon-wrapper {
  width: 80px;
  height: 80px;
  margin: 0 auto 24px;
  background: linear-gradient(135deg, #1e3c72, #2a5298);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 8px 24px rgba(42, 82, 152, 0.4);
}

.icon-wrapper svg {
  width: 40px;
  height: 40px;
  stroke: white;
  stroke-width: 2;
  fill: none;
}

h3 {
  text-align: center;
  color: #1e3c72;
  font-size: 24px;
  font-weight: 600;
  margin-bottom: 12px;
}

.subtitle {
  text-align: center;
  color: #718096;
  font-size: 14px;
  margin-bottom: 32px;
  line-height: 1.5;
}

.input-group {
  position: relative;
  margin-bottom: 24px;
}

.input-group label {
  display: block;
  color: #2d3748;
  font-size: 13px;
  font-weight: 500;
  margin-bottom: 8px;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.input-wrapper {
  position: relative;
}

.input-wrapper svg {
  position: absolute;
  left: 14px;
  top: 50%;
  transform: translateY(-50%);
  width: 20px;
  height: 20px;
  stroke: #a0aec0;
  fill: none;
  stroke-width: 2;
}

input {
  width: 100%;
  padding: 16px 16px 16px 46px;
  border-radius: 12px;
  border: 2px solid #e2e8f0;
  font-size: 15px;
  transition: all 0.3s ease;
  background: #f7fafc;
}

input:focus {
  outline: none;
  border-color: #2a5298;
  background: white;
  box-shadow: 0 0 0 3px rgba(42, 82, 152, 0.1);
}

input::placeholder {
  color: #a0aec0;
}

button {
  width: 100%;
  padding: 16px;
  background: linear-gradient(135deg, #1e3c72, #2a5298);
  color: #fff;
  border: none;
  border-radius: 12px;
  font-size: 16px;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.3s ease;
  box-shadow: 0 4px 16px rgba(42, 82, 152, 0.4);
  position: relative;
  overflow: hidden;
}

button::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
  transition: left 0.5s;
}

button:hover::before {
  left: 100%;
}

button:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 24px rgba(42, 82, 152, 0.5);
}

button:active {
  transform: translateY(0);
}

.info-box {
  background: #ebf4ff;
  border-left: 4px solid #2a5298;
  padding: 16px;
  border-radius: 8px;
  margin-top: 24px;
}

.info-box p {
  color: #2d3748;
  font-size: 13px;
  line-height: 1.6;
  text-align: left;
  margin: 0;
}

.info-box strong {
  color: #1e3c72;
}

.footer {
  text-align: center;
  padding: 24px;
  color: rgba(255, 255, 255, 0.9);
  font-size: 13px;
  position: relative;
  z-index: 10;
}

.footer p {
  margin: 4px 0;
  text-shadow: 0 1px 2px rgba(0, 0, 0, 0.2);
}

@media (max-width: 600px) {
  .header h1 {
    font-size: 22px;
  }
  
  .box {
    padding: 36px 24px;
    margin: 20px;
  }
  
  h3 {
    font-size: 20px;
  }
}
</style>
</head>

<body>
<div class="header">
  <h1>Airport Authority of India</h1>
  <p>Grievance Tracking Portal</p>
</div>

<div class="container">
  <div class="box">
    <a href="/" class="back-button">
      <svg viewBox="0 0 24 24">
        <path d="M19 12H5M12 19l-7-7 7-7"/>
      </svg>
      Back to Home
    </a>

    <div class="icon-wrapper">
      <svg viewBox="0 0 24 24">
        <path d="M9 11l3 3L22 4"></path>
        <path d="M21 12v7a2 2 0 01-2 2H5a2 2 0 01-2-2V5a2 2 0 012-2h11"></path>
      </svg>
    </div>
    
    <h3>Track Your Grievance</h3>
    <p class="subtitle">Enter your unique grievance token number below to check the current status and updates.</p>

    <form method="POST" action="/track/status">
      <div class="input-group">
        <label for="token">Grievance Token Number</label>
        <div class="input-wrapper">
          <svg viewBox="0 0 24 24">
            <rect x="3" y="11" width="18" height="11" rx="2" ry="2"></rect>
            <path d="M7 11V7a5 5 0 0110 0v4"></path>
          </svg>
          <input 
            type="text" 
            id="token"
            name="token" 
            placeholder="e.g., AAI-GRV-2026-001234" 
            required
            pattern="AAI-GRV-\d{4}-\d{6}"
            title="Please enter a valid token number (AAI-GRV-YYYY-XXXXXX)">
        </div>
      </div>

      <button type="submit">Check Status</button>
    </form>

    <div class="info-box">
      <p><strong>Need Help?</strong> Your token number was provided when you submitted your grievance. Please check your registered email or SMS for the token number.</p>
    </div>
  </div>
</div>

<div class="footer">
  <p>© 2026 Airport Authority of India</p>
  <p>Government of India</p>
</div>
</body>
</html>